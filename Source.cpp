// Main 

#include "Controller.h"
//#include <iostream>

int main()
{
	

	//sf::Sprite sprite;
	
	//sf::Color::
	//if(sprite.getLocalBounds().intersects(sprite()))
	
	//sf::Color::
	//sprite.getGlobalBounds().intersects(sprite.getGlobalBounds().)
	sf::RenderWindow window;
	
	Controller testController{ window };
	testController.displayMainMenu();
	//sf::sleep(sf::seconds(10.5));
	//testController.startGame();
	//window.
	//Controller newController;
	//newController.displayMainMenu();
	//std::cin >> wait;
	//sf::sleep(sf::seconds(10.5));
	//newController.startGame();
	return 0;
}